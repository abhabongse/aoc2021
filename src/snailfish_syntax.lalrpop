use std::str::FromStr;
use crate::snailfish::Node;

grammar;

pub Expr: Node = {
    "[" <l:Expr> "," <r:Expr> "]" => Node::new_branch(l, r),
    Num => Node::new_leaf(<>),
};

Num: i64 = {
    r"[0-9]+" => i64::from_str(<>).unwrap(),
};
